# Translation provider spec sync

## Goal

Record the new Google web-provider contracts and configuration behavior in the backend/frontend specs.

## Requirements

- Document provider IDs, canonical names, no-key configuration, GET query/token behavior, response bounds, and redaction.
- Document the five-provider configuration menu and unofficial endpoint caveat.

## Acceptance Criteria

- [ ] Backend and frontend specs match the shipped provider and UI contracts.
- [ ] Specs include focused validation and test expectations without unrelated refactors.
